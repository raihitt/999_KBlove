name: Codex PR Review Disabled

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    name: Review workflow disabled
    runs-on: ubuntu-latest
    steps:
      - name: Explain disabled state
        run: |
          echo "Automatic Codex PR review is disabled in this repository."
          echo "Reason: the current GitHub Actions path depends on API-key billing."
          echo "See CODEX_APPSERVER_OPERATION.md for the current operating model."
